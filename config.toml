[frecency]
batch_size = 20
max_rank = 1000

[temp_links]
enabled = false
default_ttl_hours = 24
max_custom_hours = 720
purge_interval_seconds = 600

[metrics]
trusted_proxy_cidrs = ["127.0.0.1/32", "::1/128"]
